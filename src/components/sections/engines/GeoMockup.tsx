"use client";

import ContextFlow from "@/components/visuals/ContextFlow";

export default function GeoMockup() {
  return (
    <div className="flex items-center justify-center py-8">
      <ContextFlow />
    </div>
  );
}
